# Emotion-Detection-from-Uploaded-Face-Images
Emotion detection from uploaded face images using deep learning. A face image is taken as input, processed using Python and OpenCV, and analyzed with the DeepFace library to identify the dominant facial emotion. The detected emotion is displayed along with the uploaded image.

